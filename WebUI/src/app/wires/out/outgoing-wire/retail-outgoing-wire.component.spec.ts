/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { RetailWiresOutComponent } from './retail-outgoing-wire.component';

describe('OutgoingWireComponent', () => {
  let component: RetailWiresOutComponent;
  let fixture: ComponentFixture<RetailWiresOutComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RetailWiresOutComponent ]
    })
    .compileComponents();
  }));

  /*beforeEach(() => {
    fixture = TestBed.createComponent(RetailWiresOutComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});